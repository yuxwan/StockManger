# 服务器配置
server:
  port: 8080
  tomcat:
    max-threads: 200
    min-spare-threads: 20

# Spring 框架配置
spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  datasource:
    url: jdbc:mysql://192.168.5.4:3306/stock?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useServerPrepStmts=true&cachePrepStmts=true&prepStmtCacheSize=256&prepStmtCacheSqlLimit=2048
    username: stock
    password: K7#mP9$vQ2@xL5!n
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 30000
      connection-timeout: 30000
      max-lifetime: 1800000
      leak-detection-threshold: 60000

  data:
    redis:
      host: 192.168.5.4
      port: 6379
      timeout: 6000ms
      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 5
          max-wait: 10000ms

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    default-property-inclusion: non_null

# Sa-Token 认证配置
sa-token:
  token-name: Authorization
  timeout: -1
  active-timeout: -1
  is-concurrent: true
  token-style: uuid

# 微信小程序配置
wx:
  miniapp:
    appid: wxb6abc5d08097c175
    secret: 4abc863864f19d83d2270583af97c947

# 静态资源配置
static:
  resource:
    url: https://yuxwan.com/static/

# 应用自定义配置
app:
  trial:
    enabled: true
    nickname: 试用用户

# 日志配置
logging:
  level:
    com.luckyun.stock: debug
    org.springframework: info
